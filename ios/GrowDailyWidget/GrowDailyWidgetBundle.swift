//
//  GrowDailyWidgetBundle.swift
//  GrowDailyWidget
//
//  Intentionally empty. The @main entry point lives in GrowDailyWidget.swift
//  instead (see GrowDailyWidgetBundle struct at the bottom of that file) -
//  keeping both would be two @main entry points in one target, which won't
//  build. This file is left in place (rather than deleted) so the Xcode
//  project's file references stay untouched.
//
